% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cMLgraph.R
\name{Graph_Estimate}
\alias{Graph_Estimate}
\title{Estimate the pairwise causal-effect graph}
\usage{
Graph_Estimate(
  b_mat,
  se_mat,
  n_vec,
  rho_mat,
  IJ_snp_list,
  t,
  random_start = 10
)
}
\arguments{
\item{b_mat}{Numeric matrix of GWAS effect estimates (rows = SNPs,
columns = traits).}

\item{se_mat}{Numeric matrix of standard errors matching `b_mat`.}

\item{n_vec}{Integer vector of GWAS sample sizes (one per trait).}

\item{rho_mat}{`N x N` correlation matrix between GWAS estimates across
traits.}

\item{IJ_snp_list}{Per-pair list of screened IV indices, as returned by
`Graph_Screen`.}

\item{t}{Instrument-selection threshold on the z-scale, passed through to
`mr_cML_O`.}

\item{random_start}{Integer; number of random starts per cML fit.}
}
\value{
A list with `obs_graph`, `obs_graph_se`, `obs_graph_pval` and the
  network-deconvolved `dir_graph`.
}
\description{
For every pair of traits `(i, j)`, runs `mr_cML_O` in both directions using
the screened IV indices, fills in the corresponding off-diagonal entries of
the observed-effect graph, and applies network deconvolution to obtain the
direct-effect graph.
}
