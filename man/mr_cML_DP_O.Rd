% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MRcML-C.R
\name{mr_cML_DP_O}
\alias{mr_cML_DP_O}
\title{MR-cML with data perturbation}
\usage{
mr_cML_DP_O(
  b_exp,
  b_out,
  se_exp,
  se_out,
  K_vec = 0:(length(b_exp) - 2),
  random_start = 0,
  random_start_pert = 0,
  maxit = 100,
  num_pert = 100,
  random_seed = 0,
  n,
  rho = 0,
  c1 = 1,
  c2 = 1,
  t = 0
)
}
\arguments{
\item{b_exp}{Numeric vector of SNP-exposure effect estimates.}

\item{b_out}{Numeric vector of SNP-outcome effect estimates.}

\item{se_exp}{Numeric vector of standard errors for `b_exp`.}

\item{se_out}{Numeric vector of standard errors for `b_out`.}

\item{K_vec}{Integer vector; candidate numbers of invalid IVs.}

\item{random_start}{Integer; number of random starts on the original data.}

\item{random_start_pert}{Integer; number of random starts per perturbed
dataset.}

\item{maxit}{Integer; maximum coordinate-descent iterations.}

\item{num_pert}{Integer; number of data-perturbation replicates.}

\item{random_seed}{Integer; if non-zero, used to seed the RNG.}

\item{n}{Integer; GWAS sample size used in the BIC penalty.}

\item{rho}{Correlation between `b_exp` and `b_out` due to sample overlap.}

\item{c1, c2}{Numeric inflation factors (>= 1) applied to `se_exp` and
`se_out` respectively.}

\item{t}{Instrument-selection threshold on the z-scale.}
}
\value{
A list combining the original `mr_cML_O` output with the DP point
  estimates, standard errors and p-values for both cML-BIC and cML-MA-BIC.
}
\description{
Runs `mr_cML_O` and supplements the result with data-perturbation (DP)
standard errors and p-values that account for selection of the invalid-IV
set.
}
