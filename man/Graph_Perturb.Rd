% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cMLgraph.R
\name{Graph_Perturb}
\alias{Graph_Perturb}
\title{GraphMRcML with data perturbation}
\usage{
Graph_Perturb(
  b_mat,
  se_mat,
  n_vec,
  rho_mat,
  IV_list,
  R_list,
  c_vec = rep(1, length(n_vec)),
  sig.cutoff = 5e-08,
  num_pert = 100,
  random_start = 10,
  seed = 0,
  trait_vec = NULL,
  curse = F
)
}
\arguments{
\item{b_mat}{Numeric matrix of GWAS effect estimates (rows = SNPs,
columns = traits). Row names must be SNP rsids.}

\item{se_mat}{Numeric matrix of standard errors matching `b_mat`.}

\item{n_vec}{Integer vector of GWAS sample sizes (one per trait).}

\item{rho_mat}{`N x N` correlation matrix between GWAS estimates across
traits.}

\item{IV_list}{List of length `N*(N-1)/2` giving candidate IV rsids for
each trait pair.}

\item{R_list}{List of LD blocks (see `Graph_Screen`).}

\item{c_vec}{Numeric vector of LDSC inflation factors (length `N`).}

\item{sig.cutoff}{Numeric; GWAS-significance threshold for instrument
selection.}

\item{num_pert}{Integer; number of data-perturbation replicates.}

\item{random_start}{Integer; number of random starts per cML fit.}

\item{seed}{Integer; RNG seed.}

\item{trait_vec}{Optional character vector of trait names; defaults to
`colnames(b_mat)`.}

\item{curse}{Logical; if `TRUE`, applies the "curse-of-winner" correction
by setting the cML threshold to `-qnorm(sig.cutoff/2)`.}
}
\value{
A list of per-perturbation observed graphs, their standard errors
  and p-values, the deconvolved direct-effect graphs, and `trait_vec`.
}
\description{
Top-level routine: screens IVs once via `Graph_Screen`, then repeatedly
generates LD-aware perturbed datasets and fits the pairwise causal-effect
graph on each one. Perturbation replicates are run in parallel via
[pbmcapply::pbmclapply()].
}
