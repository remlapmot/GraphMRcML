% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cMLgraph.R
\name{Graph_Screen}
\alias{Graph_Screen}
\title{Screen instruments and prepare LD-aware perturbation matrices}
\usage{
Graph_Screen(
  b_mat,
  se_mat,
  n_vec,
  IV_list,
  R_list,
  rho_mat,
  c_vec = rep(1, length(n_vec)),
  sig.cutoff = 5e-08
)
}
\arguments{
\item{b_mat}{Numeric matrix of GWAS effect estimates with one column per
trait. Row names must be SNP rsids.}

\item{se_mat}{Numeric matrix of standard errors matching `b_mat`.}

\item{n_vec}{Integer vector of GWAS sample sizes (one per trait).}

\item{IV_list}{List of length `N*(N-1)/2` giving candidate IV rsids for
each trait pair, in order `(1,2), (1,3), ..., (N-1,N)`.}

\item{R_list}{List of LD blocks. Each element has `R` (an LD correlation
matrix with rsid row/column names) and `snp` (the rsids in the block).}

\item{rho_mat}{`N x N` correlation matrix between GWAS estimates across
traits (e.g. from bivariate LDSC).}

\item{c_vec}{Numeric vector of inflation factors (length `N`) applied as
`sqrt(max(1, c))` to each trait's standard errors.}

\item{sig.cutoff}{Numeric; GWAS-significance threshold for instrument
selection.}
}
\value{
A list with the per-pair screened IV indices (`IJ_snp_list`), the
  per-block perturbation square-root matrices (`DP_mat_list`) and the
  (possibly inflated) `b_mat` and `se_mat`.
}
\description{
For each pair of traits, restricts the candidate IV set to SNPs that are
GWAS-significant for the exposure (and not more strongly associated with
the outcome), and pre-computes the LD-block square-root matrices used to
generate joint perturbations of the GWAS estimates. Optionally inflates the
standard errors using LDSC-style factors.
}
